// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: email_verification_tokens.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createEmailVerificationToken = `-- name: CreateEmailVerificationToken :one
INSERT INTO email_verification_tokens (user_id, token_hash, expires_at)
VALUES ($1, $2, $3)
RETURNING id, user_id, token_hash, expires_at, used_at, created_at
`

type CreateEmailVerificationTokenParams struct {
	UserID    pgtype.UUID        `json:"user_id"`
	TokenHash string             `json:"token_hash"`
	ExpiresAt pgtype.Timestamptz `json:"expires_at"`
}

func (q *Queries) CreateEmailVerificationToken(ctx context.Context, arg CreateEmailVerificationTokenParams) (EmailVerificationToken, error) {
	row := q.db.QueryRow(ctx, createEmailVerificationToken, arg.UserID, arg.TokenHash, arg.ExpiresAt)
	var i EmailVerificationToken
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TokenHash,
		&i.ExpiresAt,
		&i.UsedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getActiveEmailVerificationTokenByHash = `-- name: GetActiveEmailVerificationTokenByHash :one
SELECT id, user_id, token_hash, expires_at, used_at, created_at FROM email_verification_tokens
WHERE token_hash = $1
  AND used_at IS NULL
  AND expires_at > now()
`

// Active = not used and not expired — see
// GetActivePasswordResetTokenByHash's comment in password_reset_tokens.sql
// for why "no rows" stays undifferentiated all the way out to the API
// response.
func (q *Queries) GetActiveEmailVerificationTokenByHash(ctx context.Context, tokenHash string) (EmailVerificationToken, error) {
	row := q.db.QueryRow(ctx, getActiveEmailVerificationTokenByHash, tokenHash)
	var i EmailVerificationToken
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TokenHash,
		&i.ExpiresAt,
		&i.UsedAt,
		&i.CreatedAt,
	)
	return i, err
}

const invalidatePendingEmailVerificationTokens = `-- name: InvalidatePendingEmailVerificationTokens :exec
UPDATE email_verification_tokens
SET used_at = now()
WHERE user_id = $1 AND used_at IS NULL
`

// Called before minting a fresh verification token (registration's
// automatic first send, or an explicit POST /auth/resend-verification) so
// at most one unused verification token ever exists per user — an old,
// un-clicked link stops working the moment a newer one is issued instead
// of leaving multiple simultaneously-valid tokens outstanding.
func (q *Queries) InvalidatePendingEmailVerificationTokens(ctx context.Context, userID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, invalidatePendingEmailVerificationTokens, userID)
	return err
}

const markEmailVerificationTokenUsed = `-- name: MarkEmailVerificationTokenUsed :one
UPDATE email_verification_tokens
SET used_at = now()
WHERE id = $1 AND used_at IS NULL
RETURNING id, user_id, token_hash, expires_at, used_at, created_at
`

// Conditioned on used_at IS NULL so a concurrent replay of the same token
// loses the race cleanly (RETURNING zero rows -> pgx.ErrNoRows), same
// pattern as MarkPasswordResetTokenUsed.
func (q *Queries) MarkEmailVerificationTokenUsed(ctx context.Context, id pgtype.UUID) (EmailVerificationToken, error) {
	row := q.db.QueryRow(ctx, markEmailVerificationTokenUsed, id)
	var i EmailVerificationToken
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.TokenHash,
		&i.ExpiresAt,
		&i.UsedAt,
		&i.CreatedAt,
	)
	return i, err
}
