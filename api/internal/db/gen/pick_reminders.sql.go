// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: pick_reminders.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getNearestUnpickedGameForMembership = `-- name: GetNearestUnpickedGameForMembership :one
SELECT g.id AS game_id, g.week_id AS week_id, g.kickoff_at AS kickoff_at
FROM games g
JOIN teams ht ON ht.id = g.home_team_id
JOIN teams at ON at.id = g.away_team_id
JOIN weeks w ON w.id = g.week_id
WHERE (ht.conference = $1 OR at.conference = $1)
  AND w.season_year = $2
  AND g.kickoff_at > now()
  AND NOT EXISTS (
      SELECT 1 FROM picks p
      WHERE p.league_membership_id = $3 AND p.week_id = w.id
  )
ORDER BY g.kickoff_at ASC
LIMIT 1
`

type GetNearestUnpickedGameForMembershipParams struct {
	Conference   string      `json:"conference"`
	SeasonYear   int32       `json:"season_year"`
	MembershipID pgtype.UUID `json:"membership_id"`
}

type GetNearestUnpickedGameForMembershipRow struct {
	GameID    pgtype.UUID        `json:"game_id"`
	WeekID    pgtype.UUID        `json:"week_id"`
	KickoffAt pgtype.Timestamptz `json:"kickoff_at"`
}

// The membership's very next deadline: the soonest-kicking-off,
// conference-relevant game whose week the membership has not yet
// submitted any pick for. This is the "current week"/"nearest not-yet-
// locked game" the plan's pick_reminder trigger reminds against — a
// membership that has already picked for every upcoming week's earliest
// game gets pgx.ErrNoRows here (mapped by the caller to "nothing to
// remind, skip").
func (q *Queries) GetNearestUnpickedGameForMembership(ctx context.Context, arg GetNearestUnpickedGameForMembershipParams) (GetNearestUnpickedGameForMembershipRow, error) {
	row := q.db.QueryRow(ctx, getNearestUnpickedGameForMembership, arg.Conference, arg.SeasonYear, arg.MembershipID)
	var i GetNearestUnpickedGameForMembershipRow
	err := row.Scan(&i.GameID, &i.WeekID, &i.KickoffAt)
	return i, err
}

const listActiveContestantMembershipsForReminderScan = `-- name: ListActiveContestantMembershipsForReminderScan :many

SELECT
    lm.id AS membership_id,
    lm.user_id AS user_id,
    lm.league_id AS league_id,
    l.conference AS conference,
    l.season_year AS season_year
FROM league_memberships lm
JOIN leagues l ON l.id = lm.league_id
WHERE lm.status = 'active'
  AND lm.is_contestant = true
  AND lm.removed_at IS NULL
  AND l.status = 'active'
`

type ListActiveContestantMembershipsForReminderScanRow struct {
	MembershipID pgtype.UUID `json:"membership_id"`
	UserID       pgtype.UUID `json:"user_id"`
	LeagueID     pgtype.UUID `json:"league_id"`
	Conference   string      `json:"conference"`
	SeasonYear   int32       `json:"season_year"`
}

// Phase 7: the hourly pick_reminder scan. See internal/notify/reminder.go
// for the full design.
// Every membership the reminder scan needs to consider: an active,
// playing (is_contestant=true), non-removed member of an active league.
// Carries the league's conference/season_year alongside so the caller can
// feed them straight into GetNearestUnpickedGameForMembership without a
// second lookup per row.
func (q *Queries) ListActiveContestantMembershipsForReminderScan(ctx context.Context) ([]ListActiveContestantMembershipsForReminderScanRow, error) {
	rows, err := q.db.Query(ctx, listActiveContestantMembershipsForReminderScan)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListActiveContestantMembershipsForReminderScanRow{}
	for rows.Next() {
		var i ListActiveContestantMembershipsForReminderScanRow
		if err := rows.Scan(
			&i.MembershipID,
			&i.UserID,
			&i.LeagueID,
			&i.Conference,
			&i.SeasonYear,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
