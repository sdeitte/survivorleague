// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: week_recaps.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getLatestWeekRecapForLeague = `-- name: GetLatestWeekRecapForLeague :one
SELECT id, league_id, week_id, body, generated_at FROM week_recaps
WHERE league_id = $1
ORDER BY generated_at DESC
LIMIT 1
`

// Backs GET .../leagues/:id/recap: the most recently generated recap for
// the league, regardless of which week it was for — the Leaderboard page
// always wants "the latest thing that happened", not a specific week.
func (q *Queries) GetLatestWeekRecapForLeague(ctx context.Context, leagueID pgtype.UUID) (WeekRecap, error) {
	row := q.db.QueryRow(ctx, getLatestWeekRecapForLeague, leagueID)
	var i WeekRecap
	err := row.Scan(
		&i.ID,
		&i.LeagueID,
		&i.WeekID,
		&i.Body,
		&i.GeneratedAt,
	)
	return i, err
}

const listWeekRecapFactsForLeague = `-- name: ListWeekRecapFactsForLeague :many
SELECT
    m.id AS membership_id,
    u.display_name AS display_name,
    m.eliminated_week_id AS eliminated_week_id,
    m.bought_back AS bought_back,
    p.result AS pick_result,
    p.team_id AS team_id,
    t.name AS team_name,
    ot.name AS opponent_name,
    g.home_score AS home_score,
    g.away_score AS away_score,
    g.home_team_id AS home_team_id
FROM league_memberships m
JOIN users u ON u.id = m.user_id
LEFT JOIN picks p ON p.league_membership_id = m.id AND p.week_id = $1
LEFT JOIN games g ON g.id = p.game_id
LEFT JOIN teams t ON t.id = p.team_id
LEFT JOIN teams ot ON ot.id = (CASE WHEN g.home_team_id = p.team_id THEN g.away_team_id ELSE g.home_team_id END)
WHERE m.league_id = $2 AND m.removed_at IS NULL
ORDER BY u.display_name ASC
`

type ListWeekRecapFactsForLeagueParams struct {
	WeekID   pgtype.UUID `json:"week_id"`
	LeagueID pgtype.UUID `json:"league_id"`
}

type ListWeekRecapFactsForLeagueRow struct {
	MembershipID     pgtype.UUID `json:"membership_id"`
	DisplayName      string      `json:"display_name"`
	EliminatedWeekID pgtype.UUID `json:"eliminated_week_id"`
	BoughtBack       bool        `json:"bought_back"`
	PickResult       pgtype.Text `json:"pick_result"`
	TeamID           pgtype.UUID `json:"team_id"`
	TeamName         pgtype.Text `json:"team_name"`
	OpponentName     pgtype.Text `json:"opponent_name"`
	HomeScore        pgtype.Int4 `json:"home_score"`
	AwayScore        pgtype.Int4 `json:"away_score"`
	HomeTeamID       pgtype.UUID `json:"home_team_id"`
}

// Everything internal/recap needs to build one week's prompt facts for a
// league, in one query: every non-removed member (picked or not — a NULL
// pick_result/team_name means they missed their pick, itself a fact worth
// the recap knowing), their pick's team/opponent/final score, and whether
// THIS week is the one that eliminated them (compare eliminated_week_id
// to the week being recapped — a membership eliminated in an EARLIER week
// is simply not this week's news).
//
// home_team_id is selected as the raw nullable column, NOT a
// (g.home_team_id = p.team_id) derived boolean — sqlc's nullability
// inference doesn't reliably mark a CASE/comparison-derived boolean as
// nullable through this LEFT JOIN chain, and a member with no pick this
// week makes that comparison genuinely NULL (p.team_id is NULL), which
// would panic scanning into a non-nullable Go bool. internal/recap
// computes "picked home" itself by comparing this against team_id, both
// of which sqlc correctly infers as nullable since they're real columns —
// identical to ListPicksByMembershipForSeason's (internal/db/queries/
// picks.sql) same workaround for the same reason.
func (q *Queries) ListWeekRecapFactsForLeague(ctx context.Context, arg ListWeekRecapFactsForLeagueParams) ([]ListWeekRecapFactsForLeagueRow, error) {
	rows, err := q.db.Query(ctx, listWeekRecapFactsForLeague, arg.WeekID, arg.LeagueID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListWeekRecapFactsForLeagueRow{}
	for rows.Next() {
		var i ListWeekRecapFactsForLeagueRow
		if err := rows.Scan(
			&i.MembershipID,
			&i.DisplayName,
			&i.EliminatedWeekID,
			&i.BoughtBack,
			&i.PickResult,
			&i.TeamID,
			&i.TeamName,
			&i.OpponentName,
			&i.HomeScore,
			&i.AwayScore,
			&i.HomeTeamID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertWeekRecap = `-- name: UpsertWeekRecap :one
INSERT INTO week_recaps (league_id, week_id, body)
VALUES ($1, $2, $3)
ON CONFLICT (league_id, week_id) DO UPDATE SET
    body = EXCLUDED.body,
    generated_at = now()
RETURNING id, league_id, week_id, body, generated_at
`

type UpsertWeekRecapParams struct {
	LeagueID pgtype.UUID `json:"league_id"`
	WeekID   pgtype.UUID `json:"week_id"`
	Body     string      `json:"body"`
}

// Match/upsert on (league_id, week_id) — safe to regenerate/retry a recap
// for the same league/week (e.g. a redeploy re-running a failed
// generation), same pattern as every other sync-style upsert in this
// codebase.
func (q *Queries) UpsertWeekRecap(ctx context.Context, arg UpsertWeekRecapParams) (WeekRecap, error) {
	row := q.db.QueryRow(ctx, upsertWeekRecap, arg.LeagueID, arg.WeekID, arg.Body)
	var i WeekRecap
	err := row.Scan(
		&i.ID,
		&i.LeagueID,
		&i.WeekID,
		&i.Body,
		&i.GeneratedAt,
	)
	return i, err
}
