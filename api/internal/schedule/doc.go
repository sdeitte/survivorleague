// Package schedule handles CFBD ingestion and read access to teams, weeks,
// and games.
//
// Phase 2 adds only the canonical FBS conference list (conferences.go),
// needed by league creation before CFBD sync exists. CFBD ingestion itself
// (teams/weeks/games) lands in Phase 3 — see the roadmap in
// /Users/sdeitte/.claude/plans/witty-questing-barto.md.
package schedule
